# Jarvis's Junction — backlog

Deferred work, roughly in priority order. The dashboard sync picks this up.

## Art / theming

- **Redo all level & pack screenshots after the reskin is finished.** The splash pack
  thumbnails and any promo/OG images (`og-preview.png`, pack preview art) still show the
  current placeholder procedural tileset. Once the hand-drawn butler tileset lands, every
  level/pack screenshot needs regenerating so the marketing art matches the shipped look.
- Hand-drawn character & monster sprites (the procedural ones are placeholders).

## Front page

- The butler logo (`butler.png`) is a snapshot of the current procedural player sprite
  (tile 16,2). Regenerate it after the player art is redrawn so the logo stays in sync.
